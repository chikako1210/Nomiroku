<%= render 'reviews/link' %>


 <% @reviews.reverse_order.each do |review| %>

 <h4><%= @user.name %>さんの<%= review.prefecture %>ののみ録</h4>

  <%= @reviews.count %>件ののみ録

  <%= link_to review.title.truncate(10), review_path(review.id) %>

  <%= review.prefecture %>

  <%= review.brand %>

  <%= @user.name %>

  <%= review.created_at.strftime('%Y/%m/%d %H:%M') %>

 <% end %>