<p><%= @resource.email %>　様</p>

<p>登録メールアドレス：<%= @resource.email %></p>

<p>パスワードの再設定を行います。以下のURLより設定を行なってください。</p>


<p><%= link_to 'パスワード再設定', edit_password_url(@resource, reset_password_token: @token, host: 'http://ec2-52-198-191-23.ap-northeast-1.compute.amazonaws.com/' ) %></p>

<p>このメールに心当たりが無い場合や、パスワード変更をやめる場合は何もする必要はございません。<br />
   また、上記URLで設定を完了するまで、パスワードは変更されません。</p>

<p>サイト運営 のみ録</p>